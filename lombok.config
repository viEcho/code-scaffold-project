#此配置添加的消除是为了消除警告"Generating equals/hashCode implementation but without a call to superclass"
#使用继承了父类的子类上使用@Data @ToString @EqualsAndHashCode 注解时，IDE 会警告
config.stopBubbling=true
lombok.equalsAndHashCode.callSuper=call